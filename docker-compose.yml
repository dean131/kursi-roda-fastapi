services:
  kursi-roda-fastapi:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: kursi-roda-fastapi
    volumes:
      - .:/app
    depends_on:
      - kursi-roda-postgres
    environment:
      - DATABASE_URL=postgresql://ilham:Rahasia123@kursi-roda-postgres/kursi-roda

  kursi-roda-postgres:
    image: postgres:13-alpine
    container_name: kursi-roda-postgres
    environment:
      POSTGRES_USER: ilham
      POSTGRES_PASSWORD: Rahasia123
      POSTGRES_DB: kursi-roda
    volumes:
      - kursi-roda_data:/var/lib/postgresql/data
      
volumes:
  kursi-roda_data:

